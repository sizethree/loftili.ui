.track
  .info
    .bit.name
      p {{ track.title }}
    .bit.artist
      p {{ track.artist.name || 'not found' }}
    .bit.filetype
      p {{ track.type }}
    .bit.added
      p {{ track.createdAt | date:'shortDate' }}
  .actions
    .action
